# 更新记录

本文件记录每个版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [0.3.0] - 2026-09-08

### 新增

- 时间戳自动本地化：利用 GitHub 自带的 `<relative-time>`/`<time-ago>` 组件按 `lang` 渲染的机制，把页面语言标记为中文，"3 days ago" 等时间文案零词库成本变为中文
- 支持 Gist（`gist.github.com`）页面汉化
- 词典新增 230+ 词条，分两轮：核心交互（搜索范围、时间线动词、Issue 侧栏空态、评论框、文件视图），以及对照社区主流词库逐页比对后的补漏（通知页收件箱/筛选/通知原因、搜索过滤器标签、热门与合集、个人主页、登录注册、Copilot 仪表盘、仓库通用组件、新版用户菜单、工作流模板页），术语风格保持一致（PR/Issue/Fork/Star 等不直译）
- 弹窗显示当前版本号
- 新增 `scripts/check-dictionary.mjs` 词典完整性检查、`scripts/dedupe-dictionary.mjs` 去重工具、Playwright E2E 测试（`test/`）

### 优化

- 增量翻译：DOM 变化只翻译本批变更涉及的子树（收集新增节点 + 祖先去重 + Actions 页白名单准入），不再每次全页重扫；全量扫描仅保留给首次加载、手动触发与站内跳转
- 停用汉化时断开 MutationObserver，消除后台空转
- Actions 页面增量翻译沿用全量扫描的白名单与保护区语义

### 修复

- 修复 GitHub 新版头部的用户菜单等浮层不翻译的问题：这些浮层渲染在挂在 `body` 之外的 React portal（`#__primerPortalRoot__`）里，观察器和全量扫描原先只覆盖 `body`，改为观察/扫描 `documentElement`，浮层打开时即可增量翻译
- 修复 Actions 页面上浮层菜单不翻译的问题：原先增量翻译按"新增节点自身是否命中白名单"整体取舍，而菜单是作为一个容器节点插入的（容器不在白名单、内部链接才在），导致整棵菜单被跳过；现改为与全量扫描一致的语义——进入子树筛选白名单元素翻译，Actions 页全量扫描也同步覆盖 body 之外的浮层
- 修复后台标签页打开 GitHub 时页面保持英文、需手动点"立即作用到当前页"才翻译的问题：后台标签页的 `requestAnimationFrame` 会被挂起，现不可见时退化为定时器调度，并在标签页切回可见、浏览器前进/后退从 bfcache 恢复时自动补扫
- 修复链式重译问题：译文本身再次命中词典时会被反复重译（如 `Issues → Issue → 问题`），现在会识别并跳过脚本自身的写入，外部内容更新仍正常重译（E2E 测试发现的存量问题）
- 修复站内跳转监听无效的问题：content script 运行在隔离世界，包装 `history.pushState` 无法作用于页面主世界（旧版此路径一直是无效代码）；改为在突变批处理中检测 URL 变化 + `<title>` 专用观察器，站内跳转后的标题与新内容均可正确翻译
- 修复 `scripts/package.ps1` 因缺少 `CHANGELOG.md` 而打包失败的问题（新增本文件）；打包版本缺省时自动跟随 `manifest.json`，无需手动传参
- 修复词典值拼写错误：`"GitHub Sponsors": "GitHub ponsor"` → `"GitHub 赞助"`
- 清理词典中 882 行重复键（区段内 + 跨 `Object.assign` 区段整段复制粘贴），运行时行为经求值深度对比验证不变
- README 安装教程中硬编码的旧本机路径改为当前仓库路径

## [0.2.0] - 2026-05-09

### 新增

- 覆盖 Actions 工作流页面：运行记录、Job、Step、Runner、缓存、产物、部署审批
- Actions 页面白名单翻译策略，避免误伤日志与 YAML
- 覆盖 Settings 页面菜单与说明（权限、安全、Pages、Webhook、Secrets、Danger Zone 等）
- 弹窗开关与“立即作用到当前页”手动重译
- 兼容动态加载页面与站内无刷新跳转

## [0.1.0] - 2026-05-09

### 新增

- 首个版本：GitHub 顶部导航、仓库页、Issue、PR 等常见界面汉化
- 精确词典 + 正则规则双层翻译，跳过代码块、Markdown 正文、评论正文等用户内容
